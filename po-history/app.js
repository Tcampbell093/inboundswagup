const $=s=>document.querySelector(s);let page=1,total=0,lastRecords=[];
async function token(){const user=window.netlifyIdentity?.currentUser();if(!user)return '';return await user.jwt();}
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
function card(r,i){return `<button class="record" data-i="${i}"><div><strong>PO ${esc(r.po||'—')}</strong><div class="meta">${esc(r.delivery_id||'No delivery ID')}</div></div><div><b>${esc(r.category||'Uncategorized')}</b><div class="meta">Category</div></div><div><b>${esc(r.location||'—')}</b><div class="meta">Location</div></div><div><b>${esc(r.activity_date||'Date unavailable')}</b><div class="meta">Latest recorded activity</div></div><div><span class="pill ${r.lifecycle_state==='archived'?'archived':''}">${esc(r.lifecycle_state)}</span></div></button>`}
async function load(){const jwt=await token();if(!jwt){location.href='/login.html?redirect=/po-history/';return}$('#message').textContent='';$('#records').innerHTML='<p>Loading records…</p>';const p=new URLSearchParams({q:$('#search').value,scope:$('#scope').value,sort:$('#sort').value,page:String(page),pageSize:'40'});const res=await fetch('/api/po-history?'+p,{headers:{authorization:`Bearer ${jwt}`}});if(res.status===401){location.href='/login.html?redirect=/po-history/';return}const data=await res.json();if(!res.ok){$('#records').innerHTML='';$('#message').textContent=data.error||'Unable to load PO history.';return}lastRecords=data.records||[];total=data.total||0;$('#count').textContent=total.toLocaleString();$('#records').innerHTML=lastRecords.length?lastRecords.map(card).join(''):'<p>No matching records yet. Run the updated Excel sync to copy workbook history here.</p>';$('#page').textContent=`Page ${page} of ${Math.max(1,Math.ceil(total/40))}`;$('#prev').disabled=page<=1;$('#next').disabled=page*40>=total;const s=data.lastSync;$('#syncStatus').textContent=s?`Last workbook copy: ${new Date(s.synced_at).toLocaleString()} · ${(s.current_count+s.archive_count+s.archive_pa_count).toLocaleString()} rows`:'Waiting for the first workbook copy.';}
function details(r){$('#detailState').textContent=`${r.lifecycle_state} · ${r.source_sheet}`;$('#detailTitle').textContent=`PO ${r.po||'details'}`;const entries=Object.entries(r.row_json||{}).filter(([,v])=>String(v??'').trim());$('#detailBody').innerHTML=entries.map(([k,v])=>`<dl class="detail"><dt>${esc(k)}</dt><dd>${esc(v)}</dd></dl>`).join('');$('#details').showModal();}
document.addEventListener('click',e=>{const c=e.target.closest('.record');if(c)details(lastRecords[Number(c.dataset.i)]);});let timer;$('#search').addEventListener('input',()=>{clearTimeout(timer);timer=setTimeout(()=>{page=1;load()},250)});['scope','sort'].forEach(id=>$('#'+id).addEventListener('change',()=>{page=1;load()}));$('#refresh').onclick=load;$('#prev').onclick=()=>{page--;load()};$('#next').onclick=()=>{page++;load()};$('#close').onclick=()=>$('#details').close();window.netlifyIdentity.on('init',load);window.netlifyIdentity.init();
